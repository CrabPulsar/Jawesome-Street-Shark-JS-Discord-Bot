module.exports.run = async (bot, message, args) =>{

    message.reply("HELLO FRIEND!!!");
    
}

module.exports.help = {
    name: "hello",
    aliases: [],
    desc: "Returns a hello message."
}